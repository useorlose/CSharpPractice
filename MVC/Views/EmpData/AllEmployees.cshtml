@model List<MVC.Models.EmpTables>
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>AllEmployees</title>
</head>
<body>
    <nav>
        <div>
            @Html.ActionLink("New Registration", "NewEmployee")
        </div>
    </nav>
    <h1>List of All Employees with Us!!!</h1>
    <hr />
    <div>
        <table border="1" width="100%">
            <tr>
                <th>Employee ID</th>
                <th>Employee Name</th>
                <th>Employee Address</th>
                <th>Employee Salary</th>
                <td>Options</td>
            </tr>
            @foreach (var emp in Model)
            {
                <tr>
                    <td>@emp.EmpID</td>
                    <td>@emp.EmpName</td>
                    <td>@emp.EmpAddress</td>
                    <td>@emp.EmpSalary</td>
                    <td>
                        @Html.ActionLink("Edit", "Find", new { id = emp.EmpID })

                        @Html.ActionLink("X", "Delete", new { id = emp.EmpID })
                    </td>
                </tr>
            }
        </table>
    </div>
</body>
</html>
